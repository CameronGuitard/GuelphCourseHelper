import * as parsing from './parsing';
import * as course from './course';
import * as electron from './electron';

// add controllers and export them here as needed

export { parsing, course, electron };
